<h1>School Class Form</h1>

<%= form_for(@school_class) do |sc| %>
  <%= sc.label :title %>
  <%= sc.text_field :title %>
  <%= sc.label :room_number %>
  <%= sc.text_field :room_number %>
  <%= sc.submit %>
<% end %>
